namespace DIBBLES.Terrain.Blocks;

public class Water
{
    // Runs per-block logic, but is not instanced with per-block persistent data.
    public static void Tick(Block block, Chunk chunk)
    {
        
    }
}